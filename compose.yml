services:                                                                                                 
    api-transformer:
      image: ghcr.io/boozegar/anthroshim:latest
      ports:
        - "8001:8000"
      env_file:
        - .env
      volumes:
        - ./model-map.yml:/app/model-map.yml:ro
      restart: unless-stopped